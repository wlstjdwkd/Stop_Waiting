package com.stopwaiting.server.domain.image;

import org.springframework.data.jpa.repository.JpaRepository;

public interface WaitingInfoImageRepository extends JpaRepository<WaitingInfoImage,Long> {
}
